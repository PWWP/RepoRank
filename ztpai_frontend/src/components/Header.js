import React from 'react';

const Header = () => {
    return (
        <header className="App-header">
            {/* Dodaj tutaj zawartość nagłówka */}
        </header>
    );
};

export default Header;
